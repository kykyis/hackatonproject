class Scale {
    bg
    hand
    sheet

    constructor(bgScale, handScale, sheetScale, heartScale) {
        this.bg = bgScale
        this.hand = handScale
        this.sheet = sheetScale
        this.heart = heartScale
    }
}

export default Scale